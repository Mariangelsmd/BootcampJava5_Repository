function numeros(...valores) {
    valores.forEach((element) => {
        console.log(element);
    });
}
numeros(1, 2, 3, 4, 5);

// For each element recorre el array de valores
// valores puede ser name o cualquier otra variable
